import 'package:flutter/cupertino.dart';

class SettingPage extends StatelessWidget {
  // 추후에 Stateful 로 변경 가능성 있음. 우선 페이지 연결 기능 확인 중
  const SettingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
